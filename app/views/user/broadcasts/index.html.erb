<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-header">
        <%= link_to new_user_broadcast_path do %>
          <button type="button" class="btn btn-success mr-2"><i class="fa fa-plus"></i> 新規作成</button>
        <% end %>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-bordered table-striped index">
          <%# Header %>
            <thead>
              <tr>
                <th class="fw-150">日時</th>
                <th>タイトル</th>
                <th class="fw-120">状況</th>
                <th>配信先</th>
                <th class="fw-200"></th>
              </tr>
            </thead>
            <%# Data %>
            <tbody>
              <% @broadcasts.each do |broadcast| %>
                <tr>
                  <td>
                    <%= formatted_time(broadcast.deliver_at) || '未配信' %>
                  </td>
                  <td><%= broadcast.title %></td>
                  <td><%= render_broadcast_status(broadcast.status).html_safe %></td>
                  <td><span class="d-flex flex-wrap align-items-center"><%= render_broadcast_condition(broadcast).html_safe %></span></td>
                  <td>
                    <%= link_to user_broadcast_path(broadcast), class: "btn btn-primary btn-xs mr-2" do %>
                      詳細
                    <% end %>
                    <% if broadcast.editable? %>
                      <%= link_to edit_user_broadcast_path(broadcast), class: "btn btn-primary btn-xs mr-2" do %>
                        編集
                      <% end %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <%# Pagination %>
        <div class="d-flex justify-content-center mt-4">
          <% if @broadcasts.size == 0 %>
            <b>データはありません。</b>
          <% end %>
          <%= paginate @broadcasts %>
        </div>
      </div>
    </div>
  </div>
</div>
