  <!-- Navbar -->
  <nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
     <li class="dropdown user user-menu">
        <a class="nav-link user-panel" data-toggle="dropdown" href="#">
          <span class="hidden-xs"><%= current_user&.email %></span>
          <%- if current_user&.avatar.attached? %>
            <img src="<%= rails_blob_url(current_user&.avatar) %>" class="user-image" >
          <% else %>
            <img src="https://icon-library.com/images/user-icon-image/user-icon-image-18.jpg" class="user-image" alt="User Image">
          <% end %>
          <i class="fa fa-angle-down"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
          <%= link_to '<i class="fas fa-sign-out-alt mr-1"></i><p>ログアウト</p>'.html_safe, 
              destroy_user_session_path,
              method: :delete,
              class: "dropdown-item dropdown-footer d-flex align-items-center justify-content-center"
            %>
        </div>
      </li>
    </ul>
  </nav>
  <!-- /.navbar -->